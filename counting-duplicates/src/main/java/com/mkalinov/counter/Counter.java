package com.mkalinov.counter;

/**
 * Counter is an interface that provides a method to count the number of values in an array of integers.
 */
public interface Counter {

  int count(int[] numbers);
}